#include <stdio.h>

#include "Heap.h"

int main() {
	// This problem doesn't compile, but I think my theory behind the heap problem was correct.
	// I just wasn't able to resolve the compilation issues with pointer complications.
}